---
import type { BookPublication } from "../../types/publications";

const { title, authors, year, publisher, doi, url, openaccess } =
    Astro.props as BookPublication;
---

{authors} ({year}). <em>{title}.</em>
{publisher}.
{doi || url}
{openaccess && <i class="ai ai-open-access text-orange-600" />}
